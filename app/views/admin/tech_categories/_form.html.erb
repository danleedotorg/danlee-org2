<%= form_with(model: tech_category) do |form| %>
  <% if tech_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tech_category.errors.count, "error") %> prohibited this tech_category from being saved:</h2>

      <ul>
        <% tech_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_area :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
