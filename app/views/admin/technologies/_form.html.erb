<%= form_with(model: technology) do |form| %>
  <% if technology.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(technology.errors.count, "error") %> prohibited this technology from being saved:</h2>

      <ul>
        <% technology.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_area :name %>
  </div>

  <div>
    <%= form.label :tech_category_id, style: "display: block" %>
    <%= form.select(:tech_category_id, options_for_select(tech_categories.pluck(:name, :id))) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
